# git ignore global config
# Add it to git config 
#  (e.g. git config --global core.excludesfiles ~/.gitignore_global

# OS junk files
.DS_Store
.DS_Store?
.AppleDouble
.LSOverride

Thumbs.db
ehthumbs.db
Desktop.ini
$RECYLE.BIN/

# IDE or Editor generated files
*.swp
tags
.tern-port
 
